alias ls="ls -lG"

# alias ..="cd .."

# alias v="vim"
# alias m="mvim"

# alias ld="ls -lG | grep ^d"

# alias ll='ls -alF'
# alias la='ls -A'
# alias l='ls -CF'

# alias grep='grep --color=auto'
# alias fgrep='fgrep --color=auto'
# alias egrep='egrep --color=auto'

# alias ip="curl www.icanhazip.com"
# alias localip="ifconfig getifaddr en1"

# alias c="cd ~/code"

# alias pong="ping -c 3 www.google.com"

# alias mysql=/usr/local/mysql/bin/mysql
# alias mysqladmin=/usr/local/mysql/bin/mysqladmin

# alias path='echo -e ${PATH//:/\\n}'

# alias sayit="pbpaste | say"

# alias get="curl -O"

alias rm="rm -i"

# alias oo="open ."

# alias +x="chmod +x"

alias shs='python -m SimpleHTTPServer'

# callhub
alias callhubdb='ssh -fN -L 2222:localhost:5432 ubuntu@54.67.7.196 -i /home/init/.ssh/dbslave.pem'

# virtualenv init
alias venvinit='virtualenv env'

# pbcopy
alias pbcopy='xsel --clipboard'

# pbpaste
alias pbpaste='xsel --clipboard'
